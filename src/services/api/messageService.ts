
import { Message } from './types';

export const messageService = {
  // This is a placeholder for future message-related functionality
  // No message-related functions were in the original api.ts file
};
